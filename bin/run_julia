#!/bin/bash

if [[ $(basename $(pwd)) == "bin" ]]; then
    cd ..
fi

export MPLBACKEND=qt5agg
export JULIA_PKG_SERVER_REGISTRY_PREFERENCE=eager

julia --project -i -e 'menu() = include("examples/menu.jl")' "$@"
